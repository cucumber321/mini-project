package com.example.bics.data.schedule

enum class ScheduleStateKeys(val key: String) {
    UsersAssigned("users_assigned"),
    FilterSettings("filter_settings"),
    ChosenShiftID("chosen_shift_id")
}